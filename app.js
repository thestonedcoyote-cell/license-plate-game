(async()=>{try{const urls=['./app.bundle.part0.b64','./app.bundle.part1.b64','./app.bundle.part2.b64'];const parts=await Promise.all(urls.map(async u=>{const r=await fetch(u,{cache:'no-cache'});if(!r.ok)throw new Error(`App bundle load failed: ${r.status}`);return r.text()}));const bin=atob(parts.join(''));const bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);if(typeof DecompressionStream==='undefined')throw new Error('This browser does not support gzip decompression');const code=await new Response(new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'))).text();(0,eval)(code)}catch(e){console.error(e);document.body.insertAdjacentHTML('beforeend','<div style="padding:20px">The app failed to start. Reload once; if it persists, report it.</div>')}})();
